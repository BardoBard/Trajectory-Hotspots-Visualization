name: Build-Cmake
on:
  pull_request: {}
jobs:
  build-vcpkgs:
    runs-on:
      windows-latest
      
    steps:
    - name: vcpkg build cgal qt
      uses: johnwason/vcpkg-action@v5
      id: vcpkg
      with:
        pkgs: cgal qt
        triplet: x64-windows-release
        token: ${{ github.token }}
