name: Build-Cmake
on:
  pull_request: {}
jobs:
  build-vcpkgs:
    runs-on:
      windows-latest
      
    steps:
    - name: vcpkg build
      uses: johnwason/vcpkg-action@v5
      id: vcpkg
      with:
        pkgs: opengl qt cgal
        token: ${{ github.token }}
