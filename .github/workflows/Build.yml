name: Build-Cmake
on:
  pull_request: {}
jobs:
  build-vcpkgs:
    runs-on:
      windows-latest
      
    steps:
    - name: vcpkg build cgal
      uses: johnwason/vcpkg-action@v5
      id: vcpkg-cgal
      with:
        pkgs: cgal
        triplet: x64-windows-release
        token: ${{ github.token }}
